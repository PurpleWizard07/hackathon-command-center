# Copy this file to .env.local and fill in your Supabase project's values
# (Settings -> API in the Supabase dashboard). Leave both unset to keep
# running in local-only mode (data stays in this browser's localStorage).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
